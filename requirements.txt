# snapatch 통합 의존성 (breaker / diver / dejavu / match)
# Streamlit Cloud: Python 3.10–3.12 호환 (과도하게 최신 핀은 피함)

# --- 공통 / 웹 ---
streamlit>=1.35,<2
python-dotenv>=1.0.1
requests>=2.32.3

# --- breaker (시황 속보, Gemini) ---
# google-genai 는 diver 와 공유

# --- diver (네이버 뉴스 + Vertex AI Gemini) ---
google-genai>=1.16.1
beautifulsoup4>=4.12.3
lxml>=5.0
python-dateutil>=2.9.0
pydantic>=2.7.0

# --- dejavu (같은 종목 과거 유사 패턴) ---
pykrx>=1.0.0
pandas>=2.0.0,<3
numpy>=1.24.0,<2.5
scipy>=1.10.0,<1.16
matplotlib>=3.7.0,<4
PyYAML>=6.0
dtaidistance>=2.4.0

# --- match (유사 종목 검색, DTW) ---
fastdtw>=0.3.4
tqdm>=4.65
